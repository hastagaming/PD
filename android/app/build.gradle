plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "com.pd.app"
    compileSdk = 35

    defaultConfig {
        applicationId = "com.pd.app"
        minSdk = 26
        targetSdk = 35
        versionCode = 1
        versionName = "1.0"

        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
    }

    buildFeatures {
        viewBinding = true
    }

    signingConfigs {
        create("release") {
            // Gunakan file yang baru saja dibuat
            storeFile = file("pd-project.jks")
            // Gunakan System.getenv agar GitHub Actions bisa mengisi password secara otomatis
            storePassword = System.getenv("KEYSTORE_PASSWORD") ?: "PASSWORD_LOKAL_KAMU"
            keyAlias = System.getenv("KEY_ALIAS") ?: "pd_alias"
            keyPassword = System.getenv("KEY_PASSWORD") ?: "PASSWORD_LOKAL_KAMU"
        }
    }

    buildTypes {
        getByName("release") {
            isMinifyEnabled = false
            signingConfig = signingConfigs.getByName("release")
            // PERBAIKAN: Tanda kutip dan kurung tutup sudah ditambahkan
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
        }
    }
    
    // PERBAIKAN: Kurung kurawal '}' nyasar di atas baris ini sudah dihapus
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    sourceSets {
        getByName("main") {
            assets.srcDirs("src/main/assets")
        }
    }
}

dependencies {
    implementation("androidx.core:core-ktx:1.12.0")
    implementation("androidx.appcompat:appcompat:1.6.1")
    implementation("com.google.android.material:material:1.11.0")
    implementation("androidx.constraintlayout:constraintlayout:2.1.4")
}
